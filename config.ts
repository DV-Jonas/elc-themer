const config = {
  namespace: 'elc_tokenizer',
  key: 'tokens',
  iconPrefix: 'Icon-',
  iconPath: 'icons/',
  collectionNames: ['1.theme', '2.responsive', '3.components'],
} as const;

export default config;
