import { h } from 'preact';

const Instructions = () => {
  return (
    <div
      className={
        'p-3 h-10 text-on-surface-variant dark:text-on-surface-variant-dark'
      }
    >
      Select your frames, choose the brand, and click Apply
    </div>
  );
};

export default Instructions;
